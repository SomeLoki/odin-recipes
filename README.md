HTML pages with subpages for 3 different mexican influenced recipes


Current view of skills used:
-Git {create repository, commits, push}
-HTML basics {webpage basics, images adds, interal links, paragraphs, breaks, order and unorder lists}